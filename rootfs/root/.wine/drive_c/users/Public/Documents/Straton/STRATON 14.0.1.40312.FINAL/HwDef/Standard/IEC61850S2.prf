VAR LogicalDevice : STRING(64); (* Name of the logical device. *)
VAR LogicalNode : STRING(16); (* Name of the logical node. *)
VAR DataObject : STRING(65); (* Name of the data object. Seperate nested data objects with a . ('dot'). *)
VAR DataAttribute : STRING(65); (* Name of the data attribute. Seperate nested data attributes with a . ('dot'). *)
VAR ControlObject : BOOL := FALSE; (* Set to true, if it is an additional variable to control some feature, e.g. state-machine for control model. *)
VAR GOOSEreception : BOOL := FALSE; (* Set to true, if this variable is a GOOSE reception value. *)
VAR FunctionalConstraint : STRING(2); (* Functional constraint for data attribute. *)
VAR GOOSECB : STRING(65); (* Name of GOOSE control block for GOOSE reception variables. *)
VAR GOOSECBLD : STRING(64); (* Name of the logical device of GOOSE control block for GOOSE reception variables. *)